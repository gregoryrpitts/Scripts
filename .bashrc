alias ls='ls -al -G'

alias sda='ssh gregosh4@gregoryrpitts.com'
alias zmc='ssh zmcommun@zmcommunications.com'
alias agent='eval "$(ssh-agent -s)"'
alias glog='git log --oneline -10'

export NVM_DIR="/Users/Stanley/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm
